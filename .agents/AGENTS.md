# Project Rules - Forest Race

- **Automatic Git Commit Rule**: Whenever modifying source code, assets, or configuration files, always automatically stage (`git add .`) and commit (`git commit -m "..."`) all changes locally so the workspace is kept cleanly committed.
